.claude/

# Datenbankdateien gehören nie ins Repo (Gefahr echter Kundendaten in der
# Git-Historie) - Pfad kommt zur Laufzeit aus DB_PATH bzw. db/pack2eu.db.
*.db
*.db-shm
*.db-wal
.env

# Hinweis: node_modules/ ist bewusst NICHT ignoriert - dieses Repo
# trackt die Abhängigkeiten direkt mit (bestehende Konvention), damit
# ein Deploy nicht von einem "npm install"-Schritt abhängt.
